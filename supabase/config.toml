# Supabase-Projekt (öffentliche Referenz aus dem bestehenden Client-Bundle).
project_id = "qqkxlkrkbctexwnqjitx"

# Startpasswort 123456 (Owner): Auth-Mindestlänge 6, wie Hosted Supabase.
# (Authentication → Providers → Email → Minimum password length).
[auth]
minimum_password_length = 6

[functions.create-user]
verify_jwt = true
